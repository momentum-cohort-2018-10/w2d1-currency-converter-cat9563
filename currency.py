# Make sure than when you call convert with form and to being equal, 
# the return value is the same as value.

def convert(rates, values, form, to):
    if form == to:
        return values









# rates = [("USD", "EUR", 0.74)]

# def convert(rates, values,f,to): #from t
#     f = rates[0][0]
#     to = rates[0][1]
#     value = rates
